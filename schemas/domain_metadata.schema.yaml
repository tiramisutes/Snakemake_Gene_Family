$schema: "http://json-schema.org/draft-06/schema#"

description: an entry in the sample sheet
properties:
  Domain_ID:
    type: string
    description: Pfam id from https://pfam.xfam.org/

required:
  - Domain_ID
